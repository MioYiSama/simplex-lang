{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb-dap",
      "request": "launch",
      "name": "Debug",
      "program": "${workspaceFolder}/build/sx",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "CMake: 生成"
    }
  ]
}
